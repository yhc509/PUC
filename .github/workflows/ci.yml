name: CI

on:
  pull_request:
    branches:
      - main
      - dev
  push:
    branches:
      - main
      - dev

jobs:
  test:
    strategy:
      # Run every OS to completion so a platform-specific failure is reported
      # alongside the passing ones, not hidden behind the first cancellation.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      # Filesystem-independent check; running it once is enough.
      - name: Verify UPM .meta files
        if: matrix.os == 'ubuntu-latest'
        run: scripts/check-unity-meta.sh

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 9.0.x

      - name: Build solution
        run: dotnet build UnityCliBridge.sln -c Debug

      - name: Run tests
        run: dotnet test UnityCliBridge.sln
